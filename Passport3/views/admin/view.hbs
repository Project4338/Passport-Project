<style>
/* Reset default styles */
* .overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  display: flex;
  justify-content: center;
  align-items: center;
}

.popup {
  background-color: #fff;
  border-radius: 20px;
  padding: 20px;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
}
  .popup .close {
    position: absolute;
    top: 20px;
    right: 30px;
    transition: all 8000ms;
    font-size: 30px;
    font-weight: bold;
    text-decoration: none;
    color: #333;
  }
  .popup .close:hover {
    color: #06D85F;
  }
.blockb:hover {
  background: #06d899;
}
.blockb{
  margin-left: 40%;
  margin-top: 30px; 
   background-color: #5a04bd;
   
}
.blockb {
  display: block;
  margin: 20px auto;
}

/* ... other specific styles */
</style>
<div id="popup1" class="overlay">
	<div class="popup">
		<a class="close" href="/search">&times;</a>
		<div class="content">
		<div class="card">
    
  <img src="/images/{{user.passport_no}}.jpg" alt="Profile Photo" class="profile-photo" style="width: 150px; height: 150px; object-fit: cover;">
    <h2>{{user.first_name}}</h2>
    <p>Passport Number: <strong>{{user.passport_no}}</strong></p>
    
    <div class="details">
      <p>First Name: <span>{{user.first_name}}</span></p>
      <p>Last Name: <span>{{user.last_name}}</span></p>
      <p>Date of Birth: <span>{{user.date_of_birth}}</span></p>
      <p>Nationality: <span>{{user.nationality}}</span></p>
      <p>Gender: <span>{{user.gender}}</span></p>
      <p>Issue Date: <span>{{user.issue_date}}</span></p>
      <p>Expiry Date: <span>{{user.expiry_date}}</span></p>
      <p>Place of Issue: <span>{{user.place_of_issue}}</span></p>
 <form action="/block" method="POST">
  <input type="hidden" name="id" value='{{user.passport_no}}'>
  <button type="submit" class="blockb btn-outline-warning btn-lg" style="color: rgb(255, 255, 255);">block</button>
 </form>

    </div>
	
  </div>

		</div>